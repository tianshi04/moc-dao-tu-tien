#pragma once

// =============================================================
// secrets.h.example — File mẫu cấu hình thiết bị
// =============================================================
// HƯỚNG DẪN SỬ DỤNG:
//   1. Sao chép file này thành: src/secrets.h
//   2. Điền thông tin thực tế của bạn vào bên dưới
//   3. File secrets.h đã được .gitignore, sẽ KHÔNG bị commit lên git
//
// LẤY PLANT_CODE:
//   Admin vào Dashboard → Tạo chậu cây mới
//   Backend sinh ra plant_code ngẫu nhiên (ví dụ: "A7B3F2K9")
// =============================================================

// --- WiFi (dùng để test lần đầu hoặc fallback nếu NVS trống) ---
const char *WIFI_SSID = "Viet Hung";
const char *WIFI_PASS = "hung2132004";

// --- Plant Code mặc định (lấy từ Admin Dashboard) ---
const char *DEFAULT_PLANT_CODE = "TESTDEV1";
const char *DEFAULT_VERIFY_CODE = "123456"; // ← Điền vào đây

// --- Server ---
// Môi trường test (mock server cục bộ): "http://<IP_MAY_TINH>:8000"
//   Tìm IP bằng lệnh: ipconfig → xem IPv4 Address ở mục Wi-Fi
//   Mở cổng firewall: netsh advfirewall firewall add rule name="MockServer"
//   dir=in action=allow protocol=TCP localport=8000
//
// Môi trường production (backend thật): "https://your-domain.com"
const char *SERVER_HOST = "http://192.168.1.163:8000";

// --- MQTT Broker ---
const char *MQTT_HOST = "192.168.1.163";
const int MQTT_PORT = 1883;
const char *MQTT_USER = ""; // Để trống nếu không dùng
const char *MQTT_PASS = ""; // Để trống nếu không dùng

// --- Pin cảm biến (không cần thay đổi nếu đấu dây theo sơ đồ trong README) ---
#define PIN_DHT22 4 // DHT22 data pin
#define PIN_SOIL_ADC 32
#define PIN_OLED_SDA 21
#define PIN_OLED_SCL 22

// --- Thời gian ---
#define TELEMETRY_INTERVAL_MS 60000UL // 60 giây
